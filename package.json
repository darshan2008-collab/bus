{
  "name": "bus-attendance-system",
  "version": "1.0.0",
  "description": "College Mobile Bus Attendance Management System - Dhanalakshmi Srinivasan University",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build && npm --prefix server install",
    "start": "npm --prefix server start",
    "server": "npm --prefix server start",
    "client": "npm --prefix client run dev"
  }
}
